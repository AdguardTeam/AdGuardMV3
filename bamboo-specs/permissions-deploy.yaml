---
version: 2
deployment:
  name: browser extension mv3 - deploy
deployment-permissions:
  - groups:
      - extensions-developers
      - adguard-qa
    permissions:
      - view
environment-permissions:
  - Chrome WebStore:
      - groups:
          - extensions-developers
        permissions:
          - view
          - deploy
