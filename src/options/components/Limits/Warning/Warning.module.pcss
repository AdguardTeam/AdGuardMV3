.block {
    max-width: 720px;
    border-radius: 8px;
    padding: 24px 56px;
    background-color: var(--red700);
    color: var(--white);
    transform: translateX(-56px);
}

.paragraph {
    margin-bottom: 24px;
}

.title {
    font-size: 16px;
    font-weight: 600;
    line-height: 24px;
}

.desc {
    line-height: 18px;
    font-size: 14px;
}
