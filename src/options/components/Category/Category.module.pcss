.header {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    margin-bottom: 3.4rem;
}

.headerName {
    padding-left: 1.6rem;
    color: var(--white);
    font-size: 2.4rem;
    font-weight: 500;
}
