.header {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    margin-bottom: 3.4rem;
}

.headerName {
    padding-left: 1.6rem;
}

.headerNameIn {
    color: var(--white);
    font-size: 2.4rem;
    font-weight: 500;
}

.headerDesc {
    color: var(--graya4);
    font-size: 1.4rem;
    line-height: 16px;
}
