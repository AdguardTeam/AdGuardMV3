.rule {
    line-height: 2.4rem;
    font-size: 1.6rem;
    color: var(--white);
    letter-spacing: 0;
    display: block;
}

.ruleButton {
    text-align: left;
    font-size: 100%;
    font-family: inherit;
    border: 0;
    padding: 0;
    color: inherit;
    max-width: 100%;
    word-break: break-all;
}

.checkboxContainer {
    display: flex;
}
