.optionItem {
    display: flex;
    padding: 1.3rem 0;
    align-items: center;
    justify-content: space-between;
}

.icon {
    margin-right: 1.6rem;
}

.optionLabel {
    font-size: 1.6rem;
    color: var(--white);
    cursor: pointer;
}

.optionLabelDesc {
    font-size: 1.4rem;
    color: var(--gray8);
}
