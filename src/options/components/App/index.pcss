input {
    & ~ label {
        font-size: 2.5rem;
        color: var(--graya4);

        &::after {
            content: 'off'
        }
    }

    &:checked ~ label::after {
        content: 'on'
    }
}

