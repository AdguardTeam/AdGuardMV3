.container {
    line-height: 1.9rem;
}

.section {
    font-weight: 400;
    font-size: 1.6rem;
    color: var(--grayd8);
    letter-spacing: 0;
}

.readMoreSection {
    composes: section;
    margin: 3.4rem 0 3.2rem;
}

.readMoreLinkWrapper {
  margin-bottom: 16px;
}

.readMoreLink {
    line-height: 1.9rem;
}
