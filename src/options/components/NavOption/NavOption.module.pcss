.optionItem {
    display: flex;
    margin: 1.3rem 0;
    justify-content: space-between;
}

.option {
    display: flex;
    align-items: center;
}

.optionLabel {
    margin-left: 1.6rem;
    cursor: inherit;
}

.navLink {
    text-decoration: none;
}

.optionLabelDesc {
    font-size: 1.4rem;
    color: var(--gray700);
}
