.text {
    color: var(--grayd8);
    display: flex;
    align-items: baseline;
    font-size: 1.4rem;
    font-style: normal;
    font-weight: 400;
    line-height: 16px;
    text-align: center;
}

.container {
    composes: text;
    display: flex;
    align-items: center;
    flex-direction: column;
}

.counter {
    composes: text;
    font-weight: 600;
    font-size: 2.4rem;
    line-height: 28px;
    padding-bottom: 0.8rem;
}
