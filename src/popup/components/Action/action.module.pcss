.text {
    color: var(--grayd8);
    display: flex;
    align-items: baseline;
    font-size: 0.87rem;
    font-style: normal;
    font-weight: 400;
    line-height: 1rem;
    text-align: center;
}

.container {
    composes: text;
    display: flex;
    align-items: center;
    flex-direction: column;
}

.counter {
    composes: text;
    font-weight: 600;
    font-size: 1.5rem;
    line-height: 1.75rem;
    padding-bottom: 8px;
}
