import './styles/index.pcss';

const numbers = [1, 2, 3];

const doubledNumbers = numbers.map((number) => number * 3);

console.log(doubledNumbers); // 3, 6, 9

document.querySelector('h1').classList.add('test');
