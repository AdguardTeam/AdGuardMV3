.addRuleContainer {
    margin-bottom: 24px;
}

.addRuleButton {
    cursor: pointer;
    display: flex;
    align-items: center;
    padding-left: 0;
}

.addRule {
    font-style: normal;
    font-weight: normal;
    font-size: 1rem;
    line-height: 1.18rem;
    color: var(--green);
    padding-left: 16px;
}
