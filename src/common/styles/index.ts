import colors from './colors.module.pcss';
import common from './common.module.pcss';
import button from './button.module.pcss';
import modal from './modal.module.pcss';
import checkbox from './checkbox.module.pcss';

const theme = {
    colors,
    common,
    button,
    modal,
    checkbox,
};

export { theme };
